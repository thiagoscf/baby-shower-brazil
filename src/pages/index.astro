---
const products = [
  { id: 1, title: "Kit SOS Fraldas", image: "/gifts/fraldas.png", pix: "/pix-fraldas.png", description: "Porque acidentes acontecem... e vão acontecer MUITO! 💩", price: "R$25", bgColor: "#ffb3c6", borderColor: "#ff6699" },
  { id: 2, title: "Spa do Bebê", image: "/gifts/kit-banho.png", pix: "/pix-banho.png", description: "Sabonete, shampoo e toalhinha para transformar o banho em um dia de princesa 🛁👑", price: "R$50", bgColor: "#ffe699", borderColor: "#ffcc33" },
  { id: 3, title: "Look do Dia (Bebê Fashion Week)", image: "/gifts/roupinha.png", pix: "/pix-roupinha.png", description: "Macacãozinho fofo para estrelar o próximo desfile da sala de estar 👗✨", price: "R$100", bgColor: "#99d6ff", borderColor: "#3399ff" },
  { id: 4, title: "Combo MasterChef Baby", image: "/gifts/mamadeira.png", pix: "/pix-mamadeira.png", description: "Mamadeiras, chupetas e tudo que um mini masterchef precisa para suas receitas de leite 🍶👶", price: "R$250", bgColor: "#b3ffcc", borderColor: "#33cc66" },
  { id: 5, title: "Trono Real da Papinha", image: "/gifts/cadeira.png", pix: "/pix-cadeira.png", description: "Cadeirinha de alimentação para o bebê se sentir o rei da refeição 🍽️👑", price: "R$500", bgColor: "#e6b3ff", borderColor: "#cc66ff" },
  { id: 6, title: "Ferrari do Bebê", image: "/gifts/carrinho.png", pix: "/pix-carrinho.png", description: "Carrinho para os primeiros rolês de estilo pela vizinhança 🏎️✨", price: "R$1000", bgColor: "#ff9966", borderColor: "#cc3300" },
];

const paypalLink = "https://www.paypal.com/donate?hosted_button_id=YOUR_BUTTON_ID";

function priceColor(price) {
  const value = parseInt(price.replace(/[^\d]/g, ''), 10);
  if (value <= 100) return "#ff3399";
  if (value <= 500) return "#9900cc";
  return "#cc0000";
}

function priceLabel(price) {
  const value = parseInt(price.replace(/[^\d]/g, ''), 10);
  if (value <= 100) return "Bebê Econômico 🍼";
  if (value <= 500) return "Luxo Baby 👑";
  return "Super Investimento 💎";
}
---

<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Chá de Bebê da Emily</title>
    <style>
      body {
        font-family: 'Comic Sans MS', sans-serif;
        background: linear-gradient(to bottom, #ffe6f0, #fff0f5);
        display: flex;
        flex-direction: column;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 2rem;
        color: #333;
      }
      header { text-align: center; margin-bottom: 2rem; }
      h1 { font-size: 3rem; margin-bottom: 0.5rem; color: #ff69b4; text-shadow: 1px 1px 3px rgba(0,0,0,0.3); }
      h2 { font-size: 1.5rem; margin: 0.5rem 0; color: #ff85c1; text-shadow: 1px 1px 2px rgba(0,0,0,0.2); }
      p { color: #555; font-size: 1rem; }

      .price-label {
        font-size: 1rem;
        font-weight: bold;
        margin-bottom: 0.3rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
      }

      .price {
        font-size: 1.2rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
      }

      .product-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
        width: 100%;
        max-width: 900px;
      }

      .product-card {
        padding: 1.5rem;
        border-radius: 1.5rem;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        transition: transform 0.3s, box-shadow 0.3s;
      }

      .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      }

      .product-card img {
        width: 150px;
        height: 150px;
        object-fit: contain;
        margin-bottom: 1rem;
        border-radius: 1rem;
        border: 4px solid #ffb6c1;
        background: #fff0f5;
      }

      .icon-group {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        margin-top: 0.5rem;
      }

      .icon-link {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-decoration: none;
        color: #333;
      }

      .icon-link img {
        width: 70px;
        height: 70px;
        border-radius: 1rem;
        border: 3px solid #ffb6c1;
        background: #fff0f5;
        margin-bottom: 0.3rem;
        transition: transform 0.3s, box-shadow 0.3s;
      }

      .icon-link:hover img {
        animation: bounce 0.6s;
        box-shadow: 0 4px 12px rgba(255,105,180,0.5);
      }

      .icon-link span {
        font-weight: bold;
        color: #ff69b4;
      }

      @keyframes bounce {
        0%   { transform: translateY(0); }
        25%  { transform: translateY(-10px); }
        50%  { transform: translateY(0); }
        75%  { transform: translateY(-5px); }
        100% { transform: translateY(0); }
      }

      footer {
        margin-top: 2rem;
        text-align: center;
        font-size: 1rem;
        color: #ff69b4;
      }

      @media (max-width: 900px) { .product-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 480px) { 
        .product-grid { grid-template-columns: 1fr; } 
        .product-card img { width: 140px; height: 140px; } 
        .icon-link img { width: 60px; height: 60px; }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>🎀 Bem-vindos ao Chá de Bebê da Emily! 🎀</h1>
      <p>Escolha um presente divertido e contribua para a alegria da família! 🍼💖</p>
    </header>

    <main class="product-grid">
      {products.map((product) => (
        <div class="product-card" style={{backgroundColor: product.bgColor, border: `4px solid ${product.borderColor}`}}>
          <div>
            <img src={product.image} alt={product.title} />
            <h2>{product.title}</h2>
            <div class="price-label" style={{color: priceColor(product.price)}}>{priceLabel(product.price)}</div>
            <div class="price" style={{color: priceColor(product.price)}}>{product.price}</div>
            <p>{product.description}</p>
          </div>
          <div class="icon-group">
            <a class="icon-link" href={product.pix} target="_blank">
              <img src={product.pix} alt="PIX QR Code" />
              <span>PIX</span>
            </a>
            <a class="icon-link" href={paypalLink} target="_blank">
              <img src="/paypal-icon.png" alt="PayPal" />
              <span>PayPal</span>
            </a>
          </div>
        </div>
      ))}
    </main>

    <footer>
      Obrigado por celebrar conosco! 💖
    </footer>
  </body>
</html>
