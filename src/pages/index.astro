---
const products = [
  { 
    id: 1, 
    title: "Kit SOS Fraldas", 
    image: "/gifts/fraldas.png", 
    pix: "/pix-fraldas.png", 
    description: "Porque acidentes acontecem... e vão acontecer MUITO! 💩"
  },
  { 
    id: 2, 
    title: "Spa do Bebê", 
    image: "/gifts/kit-banho.png", 
    pix: "/pix-banho.png", 
    description: "Sabonete, shampoo e toalhinha para transformar o banho em um dia de princesa 🛁👑"
  },
  { 
    id: 3, 
    title: "Look do Dia (Bebê Fashion Week)", 
    image: "/gifts/roupinha.png", 
    pix: "/pix-roupinha.png", 
    description: "Macacãozinho fofo para estrelar o próximo desfile da sala de estar 👗✨"
  },
  { 
    id: 4, 
    title: "Combo MasterChef Baby", 
    image: "/gifts/mamadeira.png", 
    pix: "/pix-mamadeira.png", 
    description: "Mamadeiras, chupetas e tudo que um mini masterchef precisa para suas receitas de leite 🍶👶"
  },
  { 
    id: 5, 
    title: "Trono Real da Papinha", 
    image: "/gifts/cadeira.png", 
    pix: "/pix-cadeira.png", 
    description: "Cadeirinha de alimentação para o bebê se sentir o rei da refeição 🍽️👑"
  },
  { 
    id: 6, 
    title: "Ferrari do Bebê", 
    image: "/gifts/carrinho.png", 
    pix: "/pix-carrinho.png", 
    description: "Carrinho para os primeiros rolês de estilo pela vizinhança 🏎️✨"
  },
];

const paypalLink = "https://www.paypal.com/donate?hosted_button_id=YOUR_BUTTON_ID";
---

<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Chá de Bebê da Emily</title>
    <style>
      body {
        font-family: 'Comic Sans MS', sans-serif;
        background: linear-gradient(to bottom, #ffe6f0, #fff0f5);
        display: flex;
        flex-direction: column;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 2rem;
        color: #333;
      }
      header { text-align: center; margin-bottom: 2rem; }
      h1 { font-size: 3rem; margin-bottom: 0.5rem; color: #ff69b4; }
      h2 { font-size: 1.5rem; margin: 0.5rem 0; color: #ff85c1; }
      p { color: #555; font-size: 1rem; }

      .product-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
        width: 100%;
        max-width: 900px;
      }

      .product-card {
        background: white;
        padding: 1.5rem;
        border-radius: 1.5rem;
        box-shadow: 0 6px 12px rgba(0,0,0,0.1);
        text-align: center;
        transition: transform 0.3s, box-shadow 0.3s;
      }
      .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      }
      .product-card img {
        width: 150px;
        height: 150px;
        object-fit: contain;
        margin-bottom: 1rem;
        border-radius: 1rem;
        border: 4px solid #ffb6c1;
        background: #fff0f5;
      }

      /* Container dos ícones */
      .icon-group {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-top: 0.5rem;
      }

      .icon-link {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-decoration: none;
        color: #333;
        transition: transform 0.2s, box-shadow 0.2s;
      }

      .icon-link img {
        width: 70px;
        height: 70px;
        border-radius: 1rem;
        border: 3px solid #ffb6c1;
        background: #fff0f5;
        margin-bottom: 0.3rem;
        transition: transform 0.3s;
      }

      .icon-link:hover img {
        transform: scale(1.1);
        box-shadow: 0 4px 10px rgba(255,105,180,0.5);
      }

      .icon-link span {
        font-weight: bold;
        color: #ff69b4;
      }

      footer {
        margin-top: 2rem;
        text-align: center;
        font-size: 1rem;
        color: #ff69b4;
      }

      /* Responsivo */
      @media (max-width: 900px) { .product-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 480px) { 
        .product-grid { grid-template-columns: 1fr; } 
        .product-card img { width: 140px; height: 140px; } 
        .icon-link img { width: 60px; height: 60px; }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>🎀 Bem-vindos ao Chá de Bebê da Emily! 🎀</h1>
      <p>Escolha um presente divertido e contribua para a alegria da família! 🍼💖</p>
    </header>

    <main class="product-grid">
      {products.map((product) => (
        <div class="product-card">
          <img src={product.image} alt={product.title} />
          <h2>{product.title}</h2>
          <p>{product.description}</p>
          <div class="icon-group">
            {/* QR Code PIX */}
            <a class="icon-link" href={product.pix} target="_blank">
              <img src={product.pix} alt="PIX QR Code" />
              <span>PIX</span>
            </a>
            {/* PayPal */}
            <a class="icon-link" href={paypalLink} target="_blank">
              <img src="/paypal-icon.png" alt="PayPal" />
              <span>PayPal</span>
            </a>
          </div>
        </div>
      ))}
    </main>

    <footer>
      Obrigado por celebrar conosco! 💖
    </footer>
  </body>
</html>
