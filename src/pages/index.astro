---
const paypalLink = "https://www.paypal.com/donate?hosted_button_id=YOUR_BUTTON_ID";
const defaultPixQRCode = "/pix-qr.png"; // Coloque sua imagem do QR code PIX na pasta public

// Six fake products — change title, image, pix, description later
const products = [
  { id: 1, title: "Produto 1", image: "/placeholder-1.png", pix: defaultPixQRCode, description: "Descrição do produto 1 (adicione sua descrição aqui)" },
  { id: 2, title: "Produto 2", image: "/placeholder-2.png", pix: defaultPixQRCode, description: "Descrição do produto 2 (adicione sua descrição aqui)" },
  { id: 3, title: "Produto 3", image: "/placeholder-3.png", pix: defaultPixQRCode, description: "Descrição do produto 3 (adicione sua descrição aqui)" },
  { id: 4, title: "Produto 4", image: "/placeholder-4.png", pix: defaultPixQRCode, description: "Descrição do produto 4 (adicione sua descrição aqui)" },
  { id: 5, title: "Produto 5", image: "/placeholder-5.png", pix: defaultPixQRCode, description: "Descrição do produto 5 (adicione sua descrição aqui)" },
  { id: 6, title: "Produto 6", image: "/placeholder-6.png", pix: defaultPixQRCode, description: "Descrição do produto 6 (adicione sua descrição aqui)" },
];
---

<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lista de Chá de Bebê da Emily</title>
    <style>
      :root {
        --pink-1: #ff69b4;
        --pink-2: #ff85c1;
        --bg-1: #ffe6f0;
        --bg-2: #fff0f5;
      }

      body {
        font-family: 'Comic Sans MS', sans-serif;
        background: linear-gradient(to bottom, var(--bg-1), var(--bg-2));
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        min-height: 100vh;
        margin: 0;
        padding: 2rem;
        color: #333;
        overflow-x: hidden;
      }
      header {
        text-align: center;
        margin-bottom: 2rem;
        opacity: 0;
        transform: translateY(-50px);
        animation: headerFadeIn 1s forwards;
      }
      h1 { font-size: 2.25rem; margin-bottom: 0.25rem; color: var(--pink-1); }
      h2 { font-size: 1.25rem; margin-bottom: 0.5rem; color: var(--pink-2); }
      p { color: #555; margin: 0; }

      main {
        width: 100%;
        max-width: 1100px;
      }

      /* grid for products */
      .products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 1rem;
        align-items: start;
      }

      .product-card {
        background: white;
        padding: 1rem;
        border-radius: 1rem;
        box-shadow: 0 6px 12px rgba(0,0,0,0.08);
        text-align: center;
        width: 100%;
        transition: transform 0.25s, box-shadow 0.25s;
        opacity: 0;
        transform: translateY(20px);
        animation: sectionFadeIn 0.6s forwards;
      }
      .product-card:hover {
        transform: translateY(-6px);
        box-shadow: 0 12px 24px rgba(0,0,0,0.12);
      }

      .product-img {
        width: 180px;
        height: 180px;
        margin: 0.5rem auto;
        border-radius: 0.75rem;
        border: 3px solid #ffb6c1;
        object-fit: cover;
        background: #fff0f5;
      }

      .product-title {
        font-weight: 700;
        color: var(--pink-1);
        margin: 0.5rem 0;
        font-size: 1.05rem;
      }

      .product-desc {
        font-size: 0.95rem;
        color: #555;
        min-height: 44px; /* keeps cards similar height */
        margin-bottom: 0.75rem;
      }

      .buttons-row {
        display: flex;
        gap: 0.5rem;
        justify-content: center;
        flex-wrap: wrap;
      }

      .button {
        display: inline-block;
        padding: 0.5rem 0.9rem;
        background: var(--pink-1);
        color: white;
        border-radius: 0.75rem;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.95rem;
        transition: background 0.2s, transform 0.12s;
        cursor: pointer;
      }
      .button:hover { 
        background: var(--pink-2);
        transform: scale(1.03);
      }

      .button.secondary {
        background: white;
        color: var(--pink-1);
        border: 2px solid var(--pink-1);
      }
      .button.secondary:hover {
        background: var(--pink-1);
        color: white;
      }

      /* message section styles kept from original */
      .message-section {
        background: white;
        padding: 1rem;
        border-radius: 1.5rem;
        box-shadow: 0 6px 12px rgba(0,0,0,0.08);
        margin-top: 1.25rem;
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
        margin-top: 0.5rem;
      }
      input, textarea {
        padding: 0.75rem;
        border-radius: 0.75rem;
        border: 1px solid #ccc;
        font-size: 1rem;
        width: 100%;
      }
      textarea { resize: vertical; }

      #messages {
        margin-top: 1rem;
        text-align: left;
        max-height: 200px;
        overflow-y: auto;
        padding-right: 0.5rem;
      }
      .message {
        background: var(--bg-1);
        padding: 0.5rem 0.75rem;
        border-radius: 0.75rem;
        margin-bottom: 0.5rem;
        opacity: 0;
        transform: translateY(-10px);
        animation: fadeIn 0.5s forwards;
      }
      #confirmation {
        display: none;
        color: var(--pink-1);
        font-weight: bold;
        margin-top: 0.5rem;
        animation: fadeOut 3s forwards;
      }

      footer {
        margin-top: 2rem;
        text-align: center;
        font-size: 1rem;
        color: var(--pink-1);
        opacity: 0;
        transform: translateY(50px);
        animation: footerFadeIn 1s forwards;
      }

      @keyframes headerFadeIn {
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes sectionFadeIn {
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes footerFadeIn {
        to { opacity: 1; transform: translateY(0); }
      }
      @keyframes fadeOut {
        0% { opacity: 1; }
        80% { opacity: 1; }
        100% { opacity: 0; }
      }
      @keyframes fadeIn {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* small screens tweaks */
      @media (max-width: 480px) {
        .product-img { width: 140px; height: 140px; }
      }
    </style>
  </head>

  <body>
    <header>
      <h1>🎀 Bem-vindos ao Chá de Bebê da Emily! 🎀</h1>
      <p>Escolha um presente abaixo e contribua via PIX ou PayPal.</p>
    </header>

    <main>
      <section class="products-grid" aria-label="Produtos para contribuição">
        {products.map((product, idx) => (
          <article class="product-card" style={`animation-delay: ${0.08 * idx}s`}>
            <img class="product-img" src={product.image} alt={`Imagem ${product.title}`} />
            <div class="product-title">{product.title}</div>
            <div class="product-desc">{product.description}</div>

            <div class="buttons-row">
              <!-- PIX: opens the product's pix QR image in a new tab -->
              <a class="button secondary" href={product.pix} target="_blank" rel="noopener noreferrer" aria-label={`Contribuir via PIX para ${product.title}`}>
                Contribuir via PIX
              </a>

              <!-- PayPal: appends item_name to the PayPal donate link for identification -->
              <a
                class="button"
                href={`${paypalLink}&item_name=${encodeURIComponent(product.title)}`}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`Doar via PayPal para ${product.title}`}
              >
                Doar via PayPal
              </a>
            </div>
          </article>
        ))}
      </section>

      <!-- Message section (kept from original so visitors can leave messages) -->
      <section class="message-section" style="margin-top: 1.25rem;">
        <div class="icon">💌</div>
        <h2>Mensagem de Carinho</h2>
        <p>Deixe uma mensagem de carinho para o bebê e a família!</p>

        <form id="messageForm">
          <input type="text" id="name" placeholder="Seu nome" required />
          <textarea id="message" placeholder="Sua mensagem" rows="3" required></textarea>
          <button class="button" type="submit">Enviar mensagem</button>
        </form>

        <div id="confirmation">Mensagem enviada 💖</div>
        <div id="messages"></div>
      </section>
    </main>

    <footer>
      Obrigado por celebrar conosco! 💖
    </footer>

    <script>
      // Message form JS (kept same logic)
      const form = document.getElementById('messageForm');
      const messagesDiv = document.getElementById('messages');
      const confirmation = document.getElementById('confirmation');
      const maxMessages = 10;

      form.addEventListener('submit', (e) => {
        e.preventDefault();
        const name = document.getElementById('name').value.trim();
        const message = document.getElementById('message').value.trim();
        if(name && message){
          const msgDiv = document.createElement('div');
          msgDiv.className = 'message';
          msgDiv.textContent = `${name}: ${message}`;
          messagesDiv.prepend(msgDiv);

          const allMessages = messagesDiv.querySelectorAll('.message');
          if(allMessages.length > maxMessages){
            messagesDiv.removeChild(allMessages[allMessages.length - 1]);
          }

          form.reset();

          confirmation.style.display = 'block';
          setTimeout(() => {
            confirmation.style.display = 'none';
          }, 3000);

          messagesDiv.scrollTop = 0;
        }
      });
    </script>
  </body>
</html>
