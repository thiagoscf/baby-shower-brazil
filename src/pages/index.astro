---
const products = [
  { 
    id: 1, 
    title: "Kit SOS Fraldas", 
    image: "/gifts/fraldas.png", 
    pix: "/pix-fraldas.png", 
    description: "Porque acidentes acontecem... e vão acontecer MUITO! 💩",
    pixLabel: "💸 Mandar PIX de Emergência (via PIX)",
    paypalLabel: "🛒 Comprar Fraldas Online (via PayPal)",
    pixEmoji: "💥",
    paypalEmoji: "🎁"
  },
  { 
    id: 2, 
    title: "Spa do Bebê", 
    image: "/gifts/kit-banho.png", 
    pix: "/pix-banho.png", 
    description: "Sabonete, shampoo e toalhinha para transformar o banho em um dia de princesa 🛁👑",
    pixLabel: "🫧 Mandar PIX Relaxante (via PIX)",
    paypalLabel: "🧴 Patrocinar o Spa (via PayPal)",
    pixEmoji: "🛁",
    paypalEmoji: "🧴"
  },
  { 
    id: 3, 
    title: "Look do Dia (Bebê Fashion Week)", 
    image: "/gifts/roupinha.png", 
    pix: "/pix-roupinha.png", 
    description: "Macacãozinho fofo para estrelar o próximo desfile da sala de estar 👗✨",
    pixLabel: "👕 Pagar o Look via PIX",
    paypalLabel: "💃 Patrocinar o Desfile (via PayPal)",
    pixEmoji: "👕",
    paypalEmoji: "💃"
  },
  { 
    id: 4, 
    title: "Combo MasterChef Baby", 
    image: "/gifts/mamadeira.png", 
    pix: "/pix-mamadeira.png", 
    description: "Mamadeiras, chupetas e tudo que um mini masterchef precisa para suas receitas de leite 🍶👶",
    pixLabel: "🍶 Enviar PIX de Leite",
    paypalLabel: "🍽️ Comprar o Combo MasterChef (via PayPal)",
    pixEmoji: "🍶",
    paypalEmoji: "🍽️"
  },
  { 
    id: 5, 
    title: "Trono Real da Papinha", 
    image: "/gifts/cadeira.png", 
    pix: "/pix-cadeira.png", 
    description: "Cadeirinha de alimentação para o bebê se sentir o rei da refeição 🍽️👑",
    pixLabel: "👑 PIX Real",
    paypalLabel: "🍴 Pagar o Trono (via PayPal)",
    pixEmoji: "👑",
    paypalEmoji: "🍴"
  },
  { 
    id: 6, 
    title: "Ferrari do Bebê", 
    image: "/gifts/carrinho.png", 
    pix: "/pix-carrinho.png", 
    description: "Carrinho para os primeiros rolês de estilo pela vizinhança 🏎️✨",
    pixLabel: "🏎️ Turbinar com PIX",
    paypalLabel: "💰 Comprar a Ferrari (via PayPal)",
    pixEmoji: "🏎️",
    paypalEmoji: "💰"
  },
];

const paypalLink = "https://www.paypal.com/donate?hosted_button_id=YOUR_BUTTON_ID";
---

<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Chá de Bebê da Emily</title>
    <style>
      body {
        font-family: 'Comic Sans MS', sans-serif;
        background: linear-gradient(to bottom, #ffe6f0, #fff0f5);
        display: flex;
        flex-direction: column;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 2rem;
        color: #333;
      }
      header { text-align: center; margin-bottom: 2rem; }
      h1 { font-size: 3rem; margin-bottom: 0.5rem; color: #ff69b4; }
      h2 { font-size: 1.5rem; margin: 0.5rem 0; color: #ff85c1; }
      p { color: #555; font-size: 1rem; }

      .product-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
        width: 100%;
        max-width: 900px;
      }

      .product-card {
        background: white;
        padding: 1.5rem;
        border-radius: 1.5rem;
        box-shadow: 0 6px 12px rgba(0,0,0,0.1);
        text-align: center;
        transition: transform 0.3s, box-shadow 0.3s;
      }
      .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.15);
      }
      .product-card img {
        width: 150px;
        height: 150px;
        object-fit: contain;
        margin-bottom: 1rem;
        border-radius: 1rem;
        border: 4px solid #ffb6c1;
        background: #fff0f5;
      }

      /* Container dos botões */
      .button-group {
        display: flex;
        justify-content: center; /* centraliza horizontalmente */
        gap: 0.5rem; /* espaço entre os botões */
        margin-top: 0.5rem;
      }

      .button {
        flex: 1; /* faz os botões ocuparem espaço igual */
        position: relative;
        padding: 0.5rem 0.75rem;
        background: #ff69b4;
        color: white;
        border-radius: 1rem;
        text-decoration: none;
        font-weight: bold;
        font-size: 1rem;
        transition: transform 0.2s, box-shadow 0.2s;
        cursor: pointer;
      }

      .button::after {
        content: attr(data-emoji);
        position: absolute;
        top: -10px;
        right: -15px;
        font-size: 1.2rem;
        opacity: 0;
        transition: all 0.3s;
      }

      .button:hover::after {
        transform: translateY(-10px);
        opacity: 1;
      }

      .button:hover {
        animation: pulse 0.6s;
        transform: scale(1.05);
        box-shadow: 0 4px 10px rgba(255,105,180,0.6);
      }

      @keyframes pulse {
        0% { transform: scale(1); }
        50% { transform: scale(1.08); }
        100% { transform: scale(1); }
      }

      footer {
        margin-top: 2rem;
        text-align: center;
        font-size: 1rem;
        color: #ff69b4;
      }

      /* Responsivo */
      @media (max-width: 900px) { .product-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 480px) { 
        .product-grid { grid-template-columns: 1fr; } 
        .product-card img { width: 140px; height: 140px; } 
        .button-group { flex-direction: column; gap: 0.5rem; }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>🎀 Bem-vindos ao Chá de Bebê da Emily! 🎀</h1>
      <p>Escolha um presente divertido e contribua para a alegria da família! 🍼💖</p>
    </header>

    <main class="product-grid">
      {products.map((product) => (
        <div class="product-card">
          <img src={product.image} alt={product.title} />
          <h2>{product.title}</h2>
          <p>{product.description}</p>
          <div class="button-group">
            <a class="button" href={product.pix} target="_blank" data-emoji={product.pixEmoji}>{product.pixLabel}</a>
            <a class="button" href={paypalLink} target="_blank" data-emoji={product.paypalEmoji}>{product.paypalLabel}</a>
          </div>
        </div>
      ))}
    </main>

    <footer>
      Obrigado por celebrar conosco! 💖
    </footer>
  </body>
</html>
